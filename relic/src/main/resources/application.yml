server:
  port: 8172

spring:
  security:
    user:
      name: blvckmind
      password: 12345678

  profiles:
    active: dev

  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:./databases/relic-database.h2
    username: sa
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false
    hibernate:
      ddl-auto: validate

  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml
    enabled: true



